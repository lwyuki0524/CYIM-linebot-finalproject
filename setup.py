from setuptools import setup

setup(
   name='finalproject',
   version='1.0',
   description='',
   author='lwyuki',
   author_email='',
   packages=['finalproject'],  #same as name
   install_requires=[''], #external packages as dependencies
)